# Do NOT touch line endings. This is not a preference here: this
# repository publishes SHA-256 fingerprints of these files. If git turned
# LF into CRLF on checkout the bytes would change, the fingerprint would
# change, and anyone verifying would see a failure where there is none.
# Worse: they would learn to ignore the warning.
* -text
