# NO tocar los finales de linea. En este repositorio no es una manía:
# se publican huellas SHA-256 de estos ficheros, y si git convierte LF a
# CRLF al clonar, los bytes cambian, la huella cambia y quien compruebe
# ve un fallo donde no lo hay. Peor aun: se acostumbraria a ignorarlo.
* -text
